import arkos from "arkos";
import router from "@/src/router";

const app = arkos();

app.set("trust proxy", 1);

app.use(router);

{{#if (eq entryPoint "src/app")}}
app.listen();
{{else}}
export default app;
{{/if}}
