# ===========================================
# ARKOS FRAMEWORK - ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env and fill in your values
# Documentation: https://arkosjs.com/docs

# ----- SERVER CONFIGURATION -----
# Port where the server will run (default: 8000)
PORT=8000

# Host address for the server (default: localhost)
HOST=localhost

# Node environment (development, production, staging, test)
NODE_ENV=development

# ----- DATABASE CONFIGURATION -----
# Database connection URL for Prisma
# Example PostgreSQL: postgresql://user:password@localhost:5432/mydb
# Example MySQL: mysql://user:password@localhost:3306/mydb
DATABASE_URL=

# ----- AUTHENTICATION (JWT) -----
# Secret key for JWT token signing (REQUIRED in production)
# Generate a strong random string: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT token expiration time (default: 30d)
# Examples: 60s, 5m, 2h, 7d, 30d
JWT_EXPIRES_IN=30d

# JWT cookie settings
JWT_COOKIE_SECURE=false        # Set to true in production with HTTPS
JWT_COOKIE_HTTP_ONLY=true      # Prevent XSS attacks
JWT_COOKIE_SAME_SITE=lax       # Options: strict, lax, none

# ----- EMAIL CONFIGURATION (Nodemailer) -----
# SMTP server host
EMAIL_HOST=smtp.gmail.com

# SMTP server port (587 for TLS, 465 for SSL)
EMAIL_PORT=587

# Use secure connection (true for SSL/TLS)
EMAIL_SECURE=false

# Email account credentials
EMAIL_USER=your-email@example.com
EMAIL_PASSWORD=your-email-password

# Email sender name
EMAIL_NAME=Arkos App

# ----- FILE UPLOAD CONFIGURATION -----
# Base directory for file uploads (default: /uploads)
# UPLOAD_DIR=/uploads

# Maximum file size in MB
# MAX_FILE_SIZE=50

# ----- DEVELOPMENT TOOLS -----
# CLI specific port override
# CLI_PORT=

# CLI specific host override
# CLI_HOST=

# Build mode flag (set automatically during build)
# ARKOS_BUILD=

# ----- OPTIONAL: SEARCH (Algolia) -----
# ALGOLIA_APP_ID=
# ALGOLIA_SEARCH_API_KEY=
# ALGOLIA_INDEX_NAME=

# ----- OPTIONAL: CORS CONFIGURATION -----
# Allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000,https://yoursite.com

# ===========================================
# IMPORTANT SECURITY NOTES:
# - Never commit .env files to version control
# - Use strong, random values for JWT_SECRET
# - Enable JWT_COOKIE_SECURE in production
# - Rotate secrets regularly
# ===========================================